## 2024-05-24 - O(1) Cache assignment timing in double-checked locking\n**Learning:** When adding or modifying globally cached data structures initialized via double-checked locking (e.g., `_load_tools` in `backend/main.py`), you must construct the new structures locally and assign the global indicator variable (e.g., `_tools`) *last* to prevent race conditions on the fast path.\n**Action:** Assign the primary indicator variable last when populating global caches.
